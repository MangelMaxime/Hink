{
    "name": "showcase",
    "version": "1.0.0",
    "description": "Simple Fable App",
    "scripts": {
    "watch": "dotnet fable watch . --define DEBUG --run parcel ./src/index.html --out-dir dist",
    "build": "dotnet fable . && yarn run parcel build index.html --out-dir dist"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "parcel": "^1.12.4",
        "stats.js": "^0.17.0"
    }
}
